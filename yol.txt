1. gün -- 23/07

Node, React, Mongo, Visual Code kuruldu, ortam hazırlandı.

2. gün

Ön yüzde hazırlıklar yapıldı, header, footer, sayfalar hazırlandı. Login, register formları tamamlandı.

3. gün

Register için gerekli API "users" dosyası üzerinden hazırlandı, kullanıcı şeması "mongoose" paketi kullanılarak hazırlandı, back-end üzerinden MongoDB'ye bağlanıldı, ve Register API'yı ön yüzdeki register formuna bağlandı.

4. gün

"users" dosyası üzerinden Login API'yı yazıldı ve ön yüze bağlandı. "axios" kullanarak get, post istekleriyle API test edildi.

5. gün -- 27/07

Register ve Login formlarının çalıştıkları doğrulandı. Giriş yaptıktan sonra Header'ın buna göre değişmesi ayarlandı, kargo ekleme form linki de, giriş yapıldığı zaman headerda çıkacak şekilde ayarlandı. Kargo ekleme ekranı için API yazılıp, çalışması test edilip doğrulandı.

----------------------------------

6. gün -- 30/07

Kargo ekleme ekranında düzenlemeler yapıldı. Kargo takip ekranı için ön yüz yazıldı, API'da ID ile kargo bilgileri database'den çekildi.

7. gün

Kargo takip ekranında, yetkililerin kullanabileceği düzenleme önyüzü hazırlandı. Arka planda buna yönelik çalışılmaya başlandı. Ayrıca login işleminin tutulması ve programda işlenmesi için redux importuna başlandı.

8. gün

Kargo takip ekranının düzenleme tarafı yazıldı. Ayrıca aynı ekran için önyüzde css değişimi yapıldı. Daha sade ve Material Design'ı örnek alan bir tasarım yapıldı.

9. gün

Login işlemi yapıldıktan sonra redux store kullanılarak back-end tarafından üretilen tokenin kaydedilmesi sağlandı. Bunun, program her refreshlendiğinde sistem tarafından tekrardan store'a kaydedilmesi sağlandı. Düzenleme ve kargo ekleme sayfalarının sadece bu token var olduğunda kullanıcı tarafından erişilebilmesi için önyüzde düzenlemelere başlandı.

10. gün

Kargo takip sayfasındaki hatanın giderilmesi. CSS güncellemeleri.

11. gün

Sistem toplantısı. Özgür bey. Front-end tanışma. Kargo listeye kargo silme tuşu.